export const CATEGORIES = [
  { id: 'fifa_world_cup', name: 'FIFA World Cup', group: 'FIFA World Cup', flag: 'fifa', stickers: ['00', 'FWC1', 'FWC2', 'FWC3', 'FWC4', 'FWC5', 'FWC6', 'FWC7', 'FWC8', 'FWC9', 'FWC10', 'FWC11', 'FWC12', 'FWC13', 'FWC14', 'FWC15', 'FWC16', 'FWC17', 'FWC18', 'FWC19'] },
  { id: 'méxico', name: 'México', group: 'Grupo A', flag: 'mx', stickers: ['MEX1', 'MEX2', 'MEX3', 'MEX4', 'MEX5', 'MEX6', 'MEX7', 'MEX8', 'MEX9', 'MEX10', 'MEX11', 'MEX12', 'MEX13', 'MEX14', 'MEX15', 'MEX16', 'MEX17', 'MEX18', 'MEX19', 'MEX20'] },
  { id: 'áfrica_do_sul', name: 'África do Sul', group: 'Grupo A', flag: 'za', stickers: ['RSA1', 'RSA2', 'RSA3', 'RSA4', 'RSA5', 'RSA6', 'RSA7', 'RSA8', 'RSA9', 'RSA10', 'RSA11', 'RSA12', 'RSA13', 'RSA14', 'RSA15', 'RSA16', 'RSA17', 'RSA18', 'RSA19', 'RSA20'] },
  { id: 'coreia_do_sul', name: 'Coreia do Sul', group: 'Grupo A', flag: 'kr', stickers: ['KOR1', 'KOR2', 'KOR3', 'KOR4', 'KOR5', 'KOR6', 'KOR7', 'KOR8', 'KOR9', 'KOR10', 'KOR11', 'KOR12', 'KOR13', 'KOR14', 'KOR15', 'KOR16', 'KOR17', 'KOR18', 'KOR19', 'KOR20'] },
  { id: 'rep._tcheca', name: 'Rep. Tcheca', group: 'Grupo A', flag: 'cz', stickers: ['CZE1', 'CZE2', 'CZE3', 'CZE4', 'CZE5', 'CZE6', 'CZE7', 'CZE8', 'CZE9', 'CZE10', 'CZE11', 'CZE12', 'CZE13', 'CZE14', 'CZE15', 'CZE16', 'CZE17', 'CZE18', 'CZE19', 'CZE20'] },
  { id: 'canadá', name: 'Canadá', group: 'Grupo B', flag: 'ca', stickers: ['CAN1', 'CAN2', 'CAN3', 'CAN4', 'CAN5', 'CAN6', 'CAN7', 'CAN8', 'CAN9', 'CAN10', 'CAN11', 'CAN12', 'CAN13', 'CAN14', 'CAN15', 'CAN16', 'CAN17', 'CAN18', 'CAN19', 'CAN20'] },
  { id: 'bósnia', name: 'Bósnia', group: 'Grupo B', flag: 'ba', stickers: ['BIH1', 'BIH2', 'BIH3', 'BIH4', 'BIH5', 'BIH6', 'BIH7', 'BIH8', 'BIH9', 'BIH10', 'BIH11', 'BIH12', 'BIH13', 'BIH14', 'BIH15', 'BIH16', 'BIH17', 'BIH18', 'BIH19', 'BIH20'] },
  { id: 'catar', name: 'Catar', group: 'Grupo B', flag: 'qa', stickers: ['QAT1', 'QAT2', 'QAT3', 'QAT4', 'QAT5', 'QAT6', 'QAT7', 'QAT8', 'QAT9', 'QAT10', 'QAT11', 'QAT12', 'QAT13', 'QAT14', 'QAT15', 'QAT16', 'QAT17', 'QAT18', 'QAT19', 'QAT20'] },
  { id: 'suíça', name: 'Suíça', group: 'Grupo B', flag: 'ch', stickers: ['SUI1', 'SUI2', 'SUI3', 'SUI4', 'SUI5', 'SUI6', 'SUI7', 'SUI8', 'SUI9', 'SUI10', 'SUI11', 'SUI12', 'SUI13', 'SUI14', 'SUI15', 'SUI16', 'SUI17', 'SUI18', 'SUI19', 'SUI20'] },
  { id: 'brasil', name: 'Brasil', group: 'Grupo C', flag: 'br', stickers: ['BRA1', 'BRA2', 'BRA3', 'BRA4', 'BRA5', 'BRA6', 'BRA7', 'BRA8', 'BRA9', 'BRA10', 'BRA11', 'BRA12', 'BRA13', 'BRA14', 'BRA15', 'BRA16', 'BRA17', 'BRA18', 'BRA19', 'BRA20'] },
  { id: 'marrocos', name: 'Marrocos', group: 'Grupo C', flag: 'ma', stickers: ['MAR1', 'MAR2', 'MAR3', 'MAR4', 'MAR5', 'MAR6', 'MAR7', 'MAR8', 'MAR9', 'MAR10', 'MAR11', 'MAR12', 'MAR13', 'MAR14', 'MAR15', 'MAR16', 'MAR17', 'MAR18', 'MAR19', 'MAR20'] },
  { id: 'haiti', name: 'Haiti', group: 'Grupo C', flag: 'ht', stickers: ['HAI1', 'HAI2', 'HAI3', 'HAI4', 'HAI5', 'HAI6', 'HAI7', 'HAI8', 'HAI9', 'HAI10', 'HAI11', 'HAI12', 'HAI13', 'HAI14', 'HAI15', 'HAI16', 'HAI17', 'HAI18', 'HAI19', 'HAI20'] },
  { id: 'escócia', name: 'Escócia', group: 'Grupo C', flag: 'gb-sct', stickers: ['SCO1', 'SCO2', 'SCO3', 'SCO4', 'SCO5', 'SCO6', 'SCO7', 'SCO8', 'SCO9', 'SCO10', 'SCO11', 'SCO12', 'SCO13', 'SCO14', 'SCO15', 'SCO16', 'SCO17', 'SCO18', 'SCO19', 'SCO20'] },
  { id: 'estados_unidos', name: 'Estados Unidos', group: 'Grupo D', flag: 'us', stickers: ['USA1', 'USA2', 'USA3', 'USA4', 'USA5', 'USA6', 'USA7', 'USA8', 'USA9', 'USA10', 'USA11', 'USA12', 'USA13', 'USA14', 'USA15', 'USA16', 'USA17', 'USA18', 'USA19', 'USA20'] },
  { id: 'paraguai', name: 'Paraguai', group: 'Grupo D', flag: 'py', stickers: ['PAR1', 'PAR2', 'PAR3', 'PAR4', 'PAR5', 'PAR6', 'PAR7', 'PAR8', 'PAR9', 'PAR10', 'PAR11', 'PAR12', 'PAR13', 'PAR14', 'PAR15', 'PAR16', 'PAR17', 'PAR18', 'PAR19', 'PAR20'] },
  { id: 'austrália', name: 'Austrália', group: 'Grupo D', flag: 'au', stickers: ['AUS1', 'AUS2', 'AUS3', 'AUS4', 'AUS5', 'AUS6', 'AUS7', 'AUS8', 'AUS9', 'AUS10', 'AUS11', 'AUS12', 'AUS13', 'AUS14', 'AUS15', 'AUS16', 'AUS17', 'AUS18', 'AUS19', 'AUS20'] },
  { id: 'turquia', name: 'Turquia', group: 'Grupo D', flag: 'tr', stickers: ['TUR1', 'TUR2', 'TUR3', 'TUR4', 'TUR5', 'TUR6', 'TUR7', 'TUR8', 'TUR9', 'TUR10', 'TUR11', 'TUR12', 'TUR13', 'TUR14', 'TUR15', 'TUR16', 'TUR17', 'TUR18', 'TUR19', 'TUR20'] },
  { id: 'alemanha', name: 'Alemanha', group: 'Grupo E', flag: 'de', stickers: ['GER1', 'GER2', 'GER3', 'GER4', 'GER5', 'GER6', 'GER7', 'GER8', 'GER9', 'GER10', 'GER11', 'GER12', 'GER13', 'GER14', 'GER15', 'GER16', 'GER17', 'GER18', 'GER19', 'GER20'] },
  { id: 'curaçao', name: 'Curaçao', group: 'Grupo E', flag: 'cw', stickers: ['CUW1', 'CUW2', 'CUW3', 'CUW4', 'CUW5', 'CUW6', 'CUW7', 'CUW8', 'CUW9', 'CUW10', 'CUW11', 'CUW12', 'CUW13', 'CUW14', 'CUW15', 'CUW16', 'CUW17', 'CUW18', 'CUW19', 'CUW20'] },
  { id: 'costa_do_marfim', name: 'Costa do Marfim', group: 'Grupo E', flag: 'ci', stickers: ['CIV1', 'CIV2', 'CIV3', 'CIV4', 'CIV5', 'CIV6', 'CIV7', 'CIV8', 'CIV9', 'CIV10', 'CIV11', 'CIV12', 'CIV13', 'CIV14', 'CIV15', 'CIV16', 'CIV17', 'CIV18', 'CIV19', 'CIV20'] },
  { id: 'equador', name: 'Equador', group: 'Grupo E', flag: 'ec', stickers: ['ECU1', 'ECU2', 'ECU3', 'ECU4', 'ECU5', 'ECU6', 'ECU7', 'ECU8', 'ECU9', 'ECU10', 'ECU11', 'ECU12', 'ECU13', 'ECU14', 'ECU15', 'ECU16', 'ECU17', 'ECU18', 'ECU19', 'ECU20'] },
  { id: 'holanda', name: 'Holanda', group: 'Grupo F', flag: 'nl', stickers: ['NED1', 'NED2', 'NED3', 'NED4', 'NED5', 'NED6', 'NED7', 'NED8', 'NED9', 'NED10', 'NED11', 'NED12', 'NED13', 'NED14', 'NED15', 'NED16', 'NED17', 'NED18', 'NED19', 'NED20'] },
  { id: 'japão', name: 'Japão', group: 'Grupo F', flag: 'jp', stickers: ['JPN1', 'JPN2', 'JPN3', 'JPN4', 'JPN5', 'JPN6', 'JPN7', 'JPN8', 'JPN9', 'JPN10', 'JPN11', 'JPN12', 'JPN13', 'JPN14', 'JPN15', 'JPN16', 'JPN17', 'JPN18', 'JPN19', 'JPN20'] },
  { id: 'suécia', name: 'Suécia', group: 'Grupo F', flag: 'se', stickers: ['SWE1', 'SWE2', 'SWE3', 'SWE4', 'SWE5', 'SWE6', 'SWE7', 'SWE8', 'SWE9', 'SWE10', 'SWE11', 'SWE12', 'SWE13', 'SWE14', 'SWE15', 'SWE16', 'SWE17', 'SWE18', 'SWE19', 'SWE20'] },
  { id: 'tunísia', name: 'Tunísia', group: 'Grupo F', flag: 'tn', stickers: ['TUN1', 'TUN2', 'TUN3', 'TUN4', 'TUN5', 'TUN6', 'TUN7', 'TUN8', 'TUN9', 'TUN10', 'TUN11', 'TUN12', 'TUN13', 'TUN14', 'TUN15', 'TUN16', 'TUN17', 'TUN18', 'TUN19', 'TUN20'] },
  { id: 'bélgica', name: 'Bélgica', group: 'Grupo G', flag: 'be', stickers: ['BEL1', 'BEL2', 'BEL3', 'BEL4', 'BEL5', 'BEL6', 'BEL7', 'BEL8', 'BEL9', 'BEL10', 'BEL11', 'BEL12', 'BEL13', 'BEL14', 'BEL15', 'BEL16', 'BEL17', 'BEL18', 'BEL19', 'BEL20'] },
  { id: 'egito', name: 'Egito', group: 'Grupo G', flag: 'eg', stickers: ['EGY1', 'EGY2', 'EGY3', 'EGY4', 'EGY5', 'EGY6', 'EGY7', 'EGY8', 'EGY9', 'EGY10', 'EGY11', 'EGY12', 'EGY13', 'EGY14', 'EGY15', 'EGY16', 'EGY17', 'EGY18', 'EGY19', 'EGY20'] },
  { id: 'irã', name: 'Irã', group: 'Grupo G', flag: 'ir', stickers: ['IRN1', 'IRN2', 'IRN3', 'IRN4', 'IRN5', 'IRN6', 'IRN7', 'IRN8', 'IRN9', 'IRN10', 'IRN11', 'IRN12', 'IRN13', 'IRN14', 'IRN15', 'IRN16', 'IRN17', 'IRN18', 'IRN19', 'IRN20'] },
  { id: 'nova_zelândia', name: 'Nova Zelândia', group: 'Grupo G', flag: 'nz', stickers: ['NZL1', 'NZL2', 'NZL3', 'NZL4', 'NZL5', 'NZL6', 'NZL7', 'NZL8', 'NZL9', 'NZL10', 'NZL11', 'NZL12', 'NZL13', 'NZL14', 'NZL15', 'NZL16', 'NZL17', 'NZL18', 'NZL19', 'NZL20'] },
  { id: 'espanha', name: 'Espanha', group: 'Grupo H', flag: 'es', stickers: ['ESP1', 'ESP2', 'ESP3', 'ESP4', 'ESP5', 'ESP6', 'ESP7', 'ESP8', 'ESP9', 'ESP10', 'ESP11', 'ESP12', 'ESP13', 'ESP14', 'ESP15', 'ESP16', 'ESP17', 'ESP18', 'ESP19', 'ESP20'] },
  { id: 'cabo_verde', name: 'Cabo Verde', group: 'Grupo H', flag: 'cv', stickers: ['CPV1', 'CPV2', 'CPV3', 'CPV4', 'CPV5', 'CPV6', 'CPV7', 'CPV8', 'CPV9', 'CPV10', 'CPV11', 'CPV12', 'CPV13', 'CPV14', 'CPV15', 'CPV16', 'CPV17', 'CPV18', 'CPV19', 'CPV20'] },
  { id: 'arábia_saudita', name: 'Arábia Saudita', group: 'Grupo H', flag: 'sa', stickers: ['KSA1', 'KSA2', 'KSA3', 'KSA4', 'KSA5', 'KSA6', 'KSA7', 'KSA8', 'KSA9', 'KSA10', 'KSA11', 'KSA12', 'KSA13', 'KSA14', 'KSA15', 'KSA16', 'KSA17', 'KSA18', 'KSA19', 'KSA20'] },
  { id: 'uruguai', name: 'Uruguai', group: 'Grupo H', flag: 'uy', stickers: ['URU1', 'URU2', 'URU3', 'URU4', 'URU5', 'URU6', 'URU7', 'URU8', 'URU9', 'URU10', 'URU11', 'URU12', 'URU13', 'URU14', 'URU15', 'URU16', 'URU17', 'URU18', 'URU19', 'URU20'] },
  { id: 'frança', name: 'França', group: 'Grupo I', flag: 'fr', stickers: ['FRA1', 'FRA2', 'FRA3', 'FRA4', 'FRA5', 'FRA6', 'FRA7', 'FRA8', 'FRA9', 'FRA10', 'FRA11', 'FRA12', 'FRA13', 'FRA14', 'FRA15', 'FRA16', 'FRA17', 'FRA18', 'FRA19', 'FRA20'] },
  { id: 'senegal', name: 'Senegal', group: 'Grupo I', flag: 'sn', stickers: ['SEN1', 'SEN2', 'SEN3', 'SEN4', 'SEN5', 'SEN6', 'SEN7', 'SEN8', 'SEN9', 'SEN10', 'SEN11', 'SEN12', 'SEN13', 'SEN14', 'SEN15', 'SEN16', 'SEN17', 'SEN18', 'SEN19', 'SEN20'] },
  { id: 'iraque', name: 'Iraque', group: 'Grupo I', flag: 'iq', stickers: ['IRQ1', 'IRQ2', 'IRQ3', 'IRQ4', 'IRQ5', 'IRQ6', 'IRQ7', 'IRQ8', 'IRQ9', 'IRQ10', 'IRQ11', 'IRQ12', 'IRQ13', 'IRQ14', 'IRQ15', 'IRQ16', 'IRQ17', 'IRQ18', 'IRQ19', 'IRQ20'] },
  { id: 'noruega', name: 'Noruega', group: 'Grupo I', flag: 'no', stickers: ['NOR1', 'NOR2', 'NOR3', 'NOR4', 'NOR5', 'NOR6', 'NOR7', 'NOR8', 'NOR9', 'NOR10', 'NOR11', 'NOR12', 'NOR13', 'NOR14', 'NOR15', 'NOR16', 'NOR17', 'NOR18', 'NOR19', 'NOR20'] },
  { id: 'argentina', name: 'Argentina', group: 'Grupo J', flag: 'ar', stickers: ['ARG1', 'ARG2', 'ARG3', 'ARG4', 'ARG5', 'ARG6', 'ARG7', 'ARG8', 'ARG9', 'ARG10', 'ARG11', 'ARG12', 'ARG13', 'ARG14', 'ARG15', 'ARG16', 'ARG17', 'ARG18', 'ARG19', 'ARG20'] },
  { id: 'argélia', name: 'Argélia', group: 'Grupo J', flag: 'dz', stickers: ['ALG1', 'ALG2', 'ALG3', 'ALG4', 'ALG5', 'ALG6', 'ALG7', 'ALG8', 'ALG9', 'ALG10', 'ALG11', 'ALG12', 'ALG13', 'ALG14', 'ALG15', 'ALG16', 'ALG17', 'ALG18', 'ALG19', 'ALG20'] },
  { id: 'áustria', name: 'Áustria', group: 'Grupo J', flag: 'at', stickers: ['AUT1', 'AUT2', 'AUT3', 'AUT4', 'AUT5', 'AUT6', 'AUT7', 'AUT8', 'AUT9', 'AUT10', 'AUT11', 'AUT12', 'AUT13', 'AUT14', 'AUT15', 'AUT16', 'AUT17', 'AUT18', 'AUT19', 'AUT20'] },
  { id: 'jordânia', name: 'Jordânia', group: 'Grupo J', flag: 'jo', stickers: ['JOR1', 'JOR2', 'JOR3', 'JOR4', 'JOR5', 'JOR6', 'JOR7', 'JOR8', 'JOR9', 'JOR10', 'JOR11', 'JOR12', 'JOR13', 'JOR14', 'JOR15', 'JOR16', 'JOR17', 'JOR18', 'JOR19', 'JOR20'] },
  { id: 'portugal', name: 'Portugal', group: 'Grupo K', flag: 'pt', stickers: ['POR1', 'POR2', 'POR3', 'POR4', 'POR5', 'POR6', 'POR7', 'POR8', 'POR9', 'POR10', 'POR11', 'POR12', 'POR13', 'POR14', 'POR15', 'POR16', 'POR17', 'POR18', 'POR19', 'POR20'] },
  { id: 'congo', name: 'Congo', group: 'Grupo K', flag: 'cd', stickers: ['COD1', 'COD2', 'COD3', 'COD4', 'COD5', 'COD6', 'COD7', 'COD8', 'COD9', 'COD10', 'COD11', 'COD12', 'COD13', 'COD14', 'COD15', 'COD16', 'COD17', 'COD18', 'COD19', 'COD20'] },
  { id: 'uzbequistão', name: 'Uzbequistão', group: 'Grupo K', flag: 'uz', stickers: ['UZB1', 'UZB2', 'UZB3', 'UZB4', 'UZB5', 'UZB6', 'UZB7', 'UZB8', 'UZB9', 'UZB10', 'UZB11', 'UZB12', 'UZB13', 'UZB14', 'UZB15', 'UZB16', 'UZB17', 'UZB18', 'UZB19', 'UZB20'] },
  { id: 'colômbia', name: 'Colômbia', group: 'Grupo K', flag: 'co', stickers: ['COL1', 'COL2', 'COL3', 'COL4', 'COL5', 'COL6', 'COL7', 'COL8', 'COL9', 'COL10', 'COL11', 'COL12', 'COL13', 'COL14', 'COL15', 'COL16', 'COL17', 'COL18', 'COL19', 'COL20'] },
  { id: 'inglaterra', name: 'Inglaterra', group: 'Grupo L', flag: 'gb-eng', stickers: ['ENG1', 'ENG2', 'ENG3', 'ENG4', 'ENG5', 'ENG6', 'ENG7', 'ENG8', 'ENG9', 'ENG10', 'ENG11', 'ENG12', 'ENG13', 'ENG14', 'ENG15', 'ENG16', 'ENG17', 'ENG18', 'ENG19', 'ENG20'] },
  { id: 'croácia', name: 'Croácia', group: 'Grupo L', flag: 'hr', stickers: ['CRO1', 'CRO2', 'CRO3', 'CRO4', 'CRO5', 'CRO6', 'CRO7', 'CRO8', 'CRO9', 'CRO10', 'CRO11', 'CRO12', 'CRO13', 'CRO14', 'CRO15', 'CRO16', 'CRO17', 'CRO18', 'CRO19', 'CRO20'] },
  { id: 'gana', name: 'Gana', group: 'Grupo L', flag: 'gh', stickers: ['GHA1', 'GHA2', 'GHA3', 'GHA4', 'GHA5', 'GHA6', 'GHA7', 'GHA8', 'GHA9', 'GHA10', 'GHA11', 'GHA12', 'GHA13', 'GHA14', 'GHA15', 'GHA16', 'GHA17', 'GHA18', 'GHA19', 'GHA20'] },
  { id: 'panamá', name: 'Panamá', group: 'Grupo L', flag: 'pa', stickers: ['PAN1', 'PAN2', 'PAN3', 'PAN4', 'PAN5', 'PAN6', 'PAN7', 'PAN8', 'PAN9', 'PAN10', 'PAN11', 'PAN12', 'PAN13', 'PAN14', 'PAN15', 'PAN16', 'PAN17', 'PAN18', 'PAN19', 'PAN20'] },
  { id: 'coca-cola', name: 'Coca-Cola', group: 'Coca-Cola', flag: 'coca', stickers: ['CC1', 'CC2', 'CC3', 'CC4', 'CC5', 'CC6', 'CC7', 'CC8', 'CC9', 'CC10', 'CC11', 'CC12', 'CC13', 'CC14'] },
];

export const TOTAL_STICKERS = 994;

export const ALL_VALID_CODES = CATEGORIES.flatMap(cat => cat.stickers);

export const SHINY_CODES = ALL_VALID_CODES.filter(code => {
  // Sticker 00 and all FWC stickers are shiny
  if (code === '00' || code.startsWith('FWC')) return true;
  // All #1 stickers for countries (3 letters followed by '1')
  return /^[A-Z]{3}1$/.test(code);
});

export const calculateStats = (collection, settings = {}) => {
  const excludeCoca = settings?.excludeCoca === true;
  
  // Calculate total stickers and valid codes dynamically
  const categoriesToCount = excludeCoca 
    ? CATEGORIES.filter(cat => cat.id !== 'coca-cola') 
    : CATEGORIES;
  const stickersToCount = categoriesToCount.flatMap(cat => cat.stickers);
  const total = stickersToCount.length;
  
  const collected = Object.entries(collection)
    .filter(([code, s]) => s.status === 'collected' && stickersToCount.includes(code));
  const coladas = collected.length;
  
  const repetidas = Object.entries(collection)
    .filter(([code]) => stickersToCount.includes(code))
    .reduce((acc, [_, s]) => acc + (s.repeated || 0), 0);
    
  const faltando = total - coladas;
  const porcentagem = total > 0 ? ((coladas / total) * 100).toFixed(1) : 0;

  // Brilhantes
  const includedShiny = SHINY_CODES.filter(code => stickersToCount.includes(code));
  const totalBrilhantes = includedShiny.length;
  const coladasBrilhantes = collected.filter(([code]) => includedShiny.includes(code)).length;
  const porcentagemBrilhantes = totalBrilhantes > 0 ? ((coladasBrilhantes / totalBrilhantes) * 100).toFixed(1) : 0;

  return { 
    total, 
    coladas, 
    repetidas, 
    faltando, 
    porcentagem,
    totalBrilhantes,
    coladasBrilhantes,
    porcentagemBrilhantes
  };
};

export const ACHIEVEMENTS = [
  { id: 'first_10', name: 'Primeiros Passos', description: 'Cole 10 figurinhas', icon: 'Footprints' },
  { id: 'first_50', name: 'Colecionador Casual', description: 'Cole 50 figurinhas', icon: 'User' },
  { id: 'century', name: 'O Centenário', description: 'Cole 100 figurinhas', icon: 'Award' },
  { id: 'elite_300', name: 'Veterano', description: 'Cole 300 figurinhas', icon: 'Star' },
  { id: 'half_way', name: 'Meio de Campo', description: 'Complete 50% do álbum', icon: 'Target' },
  { id: 'shiny_5', name: 'Brilho Inicial', description: 'Cole 5 figurinhas brilhantes', icon: 'Sparkles' },
  { id: 'shiny_15', name: 'Mestre do Brilho', description: 'Cole 15 figurinhas brilhantes', icon: 'Zap' },
  { id: 'team_complete', name: 'Técnico', description: 'Complete 1 seleção inteira', icon: 'Shield' },
  { id: 'group_complete', name: 'Campeão de Grupo', description: 'Complete 1 grupo inteiro', icon: 'Trophy' },
  { id: 'fifa_complete', name: 'Historiador', description: 'Complete a seção FIFA', icon: 'History' },
  { id: 'coca_complete', name: 'Refrescante', description: 'Complete a seção Coca-Cola', icon: 'Flame' },
  { id: 'repeated_10', name: 'Negociador', description: 'Tenha 10 figurinhas repetidas', icon: 'RefreshCcw' },
  { id: 'trading_pro', name: 'Magnata das Trocas', description: 'Tenha 50 figurinhas repetidas', icon: 'TrendingUp' },
  { id: 'elite_500', name: 'Colecionador de Elite', description: 'Cole 500 figurinhas', icon: 'Shield' },
  { id: 'master_teams', name: 'Mestre das Seleções', description: 'Complete 10 seleções', icon: 'Trophy' },
  { id: 'globetrotter', name: 'Globetrotter', description: 'Uma de cada categoria', icon: 'Globe' },
  { id: 'master_13', name: 'Mestre das Equipes', description: 'Cole todas as fotos de equipe (nº 13)', icon: 'UsersIcon' },
  { id: 'full_album', name: 'Lenda Viva', description: 'Complete 100% do álbum', icon: 'Trophy' },
  { id: 'elite_800', name: 'Dono do Estádio', description: 'Cole 800 figurinhas', icon: 'Target' },
  { id: 'shiny_30', name: 'Galeria de Ouro', description: 'Cole 30 figurinhas brilhantes', icon: 'Sparkles' },
  { id: 'ponta_lanca', name: 'Ponta de Lança', description: 'Cole a figurinha de abertura "00"', icon: 'Star' },
  { id: 'pack_100', name: 'Diretoria', description: 'Abra mais de 100 pacotinhos', icon: 'Award' },
  { id: 'south_america', name: 'Rei das Américas', description: 'Complete todas as seleções da América do Sul', icon: 'Globe' },
  { id: 'super_negociador', name: 'Super Negociador', description: 'Tenha 100 figurinhas repetidas', icon: 'TrendingUp' },
  { id: 'coca_fifa', name: 'Parceria Suprema', description: 'Complete as seções FIFA e Coca-Cola', icon: 'Flame' },
  { id: 'first_100_percent_group', name: 'Dominador de Grupos', description: 'Complete 3 grupos inteiros', icon: 'Trophy' },
  { id: 'super_classico', name: 'Super Clássico', description: 'Complete Brasil e Argentina por inteiro', icon: 'Trophy' },
  { id: 'euro_complete', name: 'Euro Soberano', description: 'Complete as seleções da Europa', icon: 'Shield' },
  { id: 'world_champions', name: 'Clube dos Campeões', description: 'Complete as 7 seleções campeãs mundiais do álbum', icon: 'Trophy' },
  { id: 'world_tour', name: 'Volta ao Mundo', description: 'Cole figurinhas de 5 continentes', icon: 'Globe' },
  { id: 'repescagem_complete', name: 'Guerreiros da Repescagem', description: 'Complete as 6 seleções da repescagem mundial da Copa 2026', icon: 'History' },
  { id: 'hosts_complete', name: 'Anfitriões da Copa', description: 'Complete as seleções anfitriãs: México, Canadá e Estados Unidos', icon: 'Globe' }
];

export const getAchievements = (collection, settings = {}) => {
  const stats = calculateStats(collection, settings);
  
  const unlocked = new Set();

  if (stats.coladas >= 10) unlocked.add('first_10');
  if (stats.coladas >= 50) unlocked.add('first_50');
  if (stats.coladas >= 100) unlocked.add('century');
  if (stats.coladas >= 300) unlocked.add('elite_300');
  if (stats.coladas >= 500) unlocked.add('elite_500');
  if (stats.coladas >= 800) unlocked.add('elite_800');
  if (stats.coladas / stats.total >= 0.5) unlocked.add('half_way');
  if (stats.coladasBrilhantes >= 5) unlocked.add('shiny_5');
  if (stats.coladasBrilhantes >= 15) unlocked.add('shiny_15');
  if (stats.coladasBrilhantes >= 30) unlocked.add('shiny_30');
  if (stats.repetidas >= 10) unlocked.add('repeated_10');
  if (stats.repetidas >= 50) unlocked.add('trading_pro');
  if (stats.repetidas >= 100) unlocked.add('super_negociador');
  if (collection['00']?.status === 'collected') unlocked.add('ponta_lanca');
  if ((stats.coladas + stats.repetidas) >= 500) unlocked.add('pack_100');
  const wcBraComplete = CATEGORIES.find(c => c.id === 'brasil')?.stickers.every(code => collection[code]?.status === 'collected');
  const wcArgComplete = CATEGORIES.find(c => c.id === 'argentina')?.stickers.every(code => collection[code]?.status === 'collected');
  const wcGerComplete = CATEGORIES.find(c => c.id === 'alemanha')?.stickers.every(code => collection[code]?.status === 'collected');
  const wcFraComplete = CATEGORIES.find(c => c.id === 'frança')?.stickers.every(code => collection[code]?.status === 'collected');
  const wcUyComplete = CATEGORIES.find(c => c.id === 'uruguai')?.stickers.every(code => collection[code]?.status === 'collected');
  const wcEspComplete = CATEGORIES.find(c => c.id === 'espanha')?.stickers.every(code => collection[code]?.status === 'collected');
  const wcEngComplete = CATEGORIES.find(c => c.id === 'inglaterra')?.stickers.every(code => collection[code]?.status === 'collected');

  if (wcBraComplete && wcArgComplete) unlocked.add('super_classico');
  if (wcBraComplete && wcArgComplete && wcGerComplete && wcFraComplete && wcUyComplete && wcEspComplete && wcEngComplete) unlocked.add('world_champions');

  // Check if any team is complete
  let completeTeams = 0;
  let hasFromAll = true;
  const categoriesToCheck = settings?.excludeCoca === true
    ? CATEGORIES.filter(c => c.id !== 'coca-cola')
    : CATEGORIES;

  for (const cat of categoriesToCheck) {
    const collectedInCat = cat.stickers.filter(code => collection[code]?.status === 'collected');
    if (collectedInCat.length === 0) hasFromAll = false;
    
    if (collectedInCat.length === cat.stickers.length) {
      completeTeams++;
      unlocked.add('team_complete');
      if (cat.id === 'fifa_world_cup') unlocked.add('fifa_complete');
      if (cat.id === 'coca-cola') unlocked.add('coca_complete');
    }
  }
  
  if (completeTeams >= 10) unlocked.add('master_teams');
  if (hasFromAll) unlocked.add('globetrotter');
  if (stats.coladas === stats.total) unlocked.add('full_album');

  // Check Master 13
  const codes13 = ALL_VALID_CODES.filter(code => code.endsWith('13'));
  const collected13 = codes13.filter(code => collection[code]?.status === 'collected');
  if (collected13.length === codes13.length) unlocked.add('master_13');

  // Check groups
  const groups = [...new Set(CATEGORIES.map(c => c.group))];
  for (const group of groups) {
    const groupCategories = CATEGORIES.filter(c => c.group === group);
    const isGroupComplete = groupCategories.every(cat => 
      cat.stickers.every(code => collection[code]?.status === 'collected')
    );
    if (isGroupComplete && group.startsWith('Grupo')) unlocked.add('group_complete');
  }

  // South America complete (Brasil, Argentina, Uruguai, Paraguai, Equador, Colômbia)
  const brComplete = CATEGORIES.find(c => c.id === 'brasil')?.stickers.every(code => collection[code]?.status === 'collected');
  const arComplete = CATEGORIES.find(c => c.id === 'argentina')?.stickers.every(code => collection[code]?.status === 'collected');
  const uyComplete = CATEGORIES.find(c => c.id === 'uruguai')?.stickers.every(code => collection[code]?.status === 'collected');
  const pyComplete = CATEGORIES.find(c => c.id === 'paraguai')?.stickers.every(code => collection[code]?.status === 'collected');
  const ecComplete = CATEGORIES.find(c => c.id === 'equador')?.stickers.every(code => collection[code]?.status === 'collected');
  const coComplete = CATEGORIES.find(c => c.id === 'colômbia')?.stickers.every(code => collection[code]?.status === 'collected');
  if (brComplete && arComplete && uyComplete && pyComplete && ecComplete && coComplete) unlocked.add('south_america');
  
  // Coca + FIFA complete
  const fifaComplete = CATEGORIES.find(c => c.id === 'fifa_world_cup')?.stickers.every(code => collection[code]?.status === 'collected');
  const cocaComplete = CATEGORIES.find(c => c.id === 'coca-cola')?.stickers.every(code => collection[code]?.status === 'collected');
  if (fifaComplete && cocaComplete) unlocked.add('coca_fifa');

  // Complete 3 Groups
  const groupList = [...new Set(CATEGORIES.map(c => c.group).filter(g => g.startsWith('Grupo')))];
  let completeGroups = 0;
  for (const group of groupList) {
    const groupCats = CATEGORIES.filter(c => c.group === group);
    const isGroupComplete = groupCats.every(cat => 
      cat.stickers.every(code => collection[code]?.status === 'collected')
    );
    if (isGroupComplete) completeGroups++;
  }
  if (completeGroups >= 3) unlocked.add('first_100_percent_group');

  // Euro complete
  const euroCats = ['rep._tcheca', 'suíça', 'escócia', 'bósnia', 'alemanha', 'holanda', 'suécia', 'bélgica', 'espanha', 'frança', 'noruega', 'áustria', 'portugal', 'inglaterra', 'croácia'];
  const euroComplete = euroCats.every(catId => 
    CATEGORIES.find(c => c.id === catId)?.stickers.every(code => collection[code]?.status === 'collected')
  );
  if (euroComplete) unlocked.add('euro_complete');

  // World Tour (5 continents)
  const saCats = ['brasil', 'argentina', 'uruguai', 'paraguai', 'equador', 'colômbia'];
  const naCats = ['méxico', 'canadá', 'estados_unidos', 'haiti', 'curaçao', 'panamá'];
  const euCats = ['rep._tcheca', 'suíça', 'escócia', 'bósnia', 'alemanha', 'holanda', 'suécia', 'bélgica', 'espanha', 'frança', 'noruega', 'áustria', 'portugal', 'inglaterra', 'croácia'];
  const afCats = ['áfrica_do_sul', 'marrocos', 'costa_do_marfim', 'tunísia', 'egito', 'senegal', 'argélia', 'congo', 'cabo_verde', 'gana'];
  const asCats = ['coreia_do_sul', 'catar', 'austrália', 'japão', 'irã', 'nova_zelândia', 'arábia_saudita', 'iraque', 'jordânia', 'uzbequistão'];
  const hasSa = saCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  const hasNa = naCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  const hasEu = euCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  const hasAf = afCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  const hasAs = asCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  if (hasSa && hasNa && hasEu && hasAf && hasAs) unlocked.add('world_tour');

  // Repescagem complete (rep._tcheca, bósnia, turquia, suécia, iraque, congo)
  const playoffCats = ['rep._tcheca', 'bósnia', 'turquia', 'suécia', 'iraque', 'congo'];
  const playoffComplete = playoffCats.every(catId => 
    CATEGORIES.find(c => c.id === catId)?.stickers.every(code => collection[code]?.status === 'collected')
  );
  if (playoffComplete) unlocked.add('repescagem_complete');

  // Hosts complete (México, Canadá, Estados Unidos)
  const mexComplete = CATEGORIES.find(c => c.id === 'méxico')?.stickers.every(code => collection[code]?.status === 'collected');
  const canComplete = CATEGORIES.find(c => c.id === 'canadá')?.stickers.every(code => collection[code]?.status === 'collected');
  const usaComplete = CATEGORIES.find(c => c.id === 'estados_unidos')?.stickers.every(code => collection[code]?.status === 'collected');
  if (mexComplete && canComplete && usaComplete) unlocked.add('hosts_complete');

  return unlocked;
};

export const getAchievementProgress = (collection, settings = {}) => {
  const stats = calculateStats(collection, settings);
  const progress = {};

  const wcBrStickers = CATEGORIES.find(c => c.id === 'brasil')?.stickers || [];
  const wcArStickers = CATEGORIES.find(c => c.id === 'argentina')?.stickers || [];
  const wcEspStickers = CATEGORIES.find(c => c.id === 'espanha')?.stickers || [];
  const wcFraStickers = CATEGORIES.find(c => c.id === 'frança')?.stickers || [];
  const wcGerStickers = CATEGORIES.find(c => c.id === 'alemanha')?.stickers || [];
  const wcUyStickers = CATEGORIES.find(c => c.id === 'uruguai')?.stickers || [];
  const wcEngStickers = CATEGORIES.find(c => c.id === 'inglaterra')?.stickers || [];

  progress['first_10'] = { current: stats.coladas, target: 10 };
  progress['first_50'] = { current: stats.coladas, target: 50 };
  progress['century'] = { current: stats.coladas, target: 100 };
  progress['elite_300'] = { current: stats.coladas, target: 300 };
  progress['elite_500'] = { current: stats.coladas, target: 500 };
  progress['elite_800'] = { current: stats.coladas, target: 800 };
  progress['half_way'] = { current: Math.min(100, Math.floor((stats.coladas / stats.total) * 100)), target: 50, isPercent: true };
  progress['shiny_5'] = { current: stats.coladasBrilhantes, target: 5 };
  progress['shiny_15'] = { current: stats.coladasBrilhantes, target: 15 };
  progress['shiny_30'] = { current: stats.coladasBrilhantes, target: 30 };
  progress['repeated_10'] = { current: stats.repetidas, target: 10 };
  progress['trading_pro'] = { current: stats.repetidas, target: 50 };
  progress['super_negociador'] = { current: stats.repetidas, target: 100 };
  progress['full_album'] = { current: stats.coladas, target: stats.total };
  progress['ponta_lanca'] = { current: collection['00']?.status === 'collected' ? 1 : 0, target: 1 };
  progress['pack_100'] = { current: stats.coladas + stats.repetidas, target: 500 };
  // Super Classico progress
  const scTotal = [...wcBrStickers, ...wcArStickers];
  const scCollected = scTotal.filter(code => collection[code]?.status === 'collected').length;
  progress['super_classico'] = { current: scCollected, target: scTotal.length };

  // World Champions progress
  const wcTotal = [...wcBrStickers, ...wcArStickers, ...wcGerStickers, ...wcFraStickers, ...wcUyStickers, ...wcEspStickers, ...wcEngStickers];
  const wcCollected = wcTotal.filter(code => collection[code]?.status === 'collected').length;
  progress['world_champions'] = { current: wcCollected, target: wcTotal.length };
  
  // South America progress (Brasil, Argentina, Uruguai, Paraguai, Equador, Colômbia)
  const pyStickers = CATEGORIES.find(c => c.id === 'paraguai')?.stickers || [];
  const ecStickers = CATEGORIES.find(c => c.id === 'equador')?.stickers || [];
  const coStickers = CATEGORIES.find(c => c.id === 'colômbia')?.stickers || [];
  const saTotalStickers = [...wcBrStickers, ...wcArStickers, ...wcUyStickers, ...pyStickers, ...ecStickers, ...coStickers];
  const saCollected = saTotalStickers.filter(code => collection[code]?.status === 'collected').length;
  progress['south_america'] = { current: saCollected, target: saTotalStickers.length };
  
  // Coca + FIFA progress
  const fifaStickers = CATEGORIES.find(c => c.id === 'fifa_world_cup')?.stickers || [];
  const cocaStickers = CATEGORIES.find(c => c.id === 'coca-cola')?.stickers || [];
  const cfTotalStickers = [...fifaStickers, ...cocaStickers];
  const cfCollected = cfTotalStickers.filter(code => collection[code]?.status === 'collected').length;
  progress['coca_fifa'] = { current: cfCollected, target: cfTotalStickers.length };

  let completeTeams = 0;
  let categoriesWithStickers = 0;
  const categoriesToCheckProgress = settings?.excludeCoca === true
    ? CATEGORIES.filter(c => c.id !== 'coca-cola')
    : CATEGORIES;
  for (const cat of categoriesToCheckProgress) {
    const collectedInCat = cat.stickers.filter(code => collection[code]?.status === 'collected');
    if (collectedInCat.length > 0) categoriesWithStickers++;
    if (collectedInCat.length === cat.stickers.length) completeTeams++;
  }

  progress['team_complete'] = { current: completeTeams, target: 1 };
  progress['master_teams'] = { current: completeTeams, target: 10 };
  progress['globetrotter'] = { current: categoriesWithStickers, target: categoriesToCheckProgress.length };
  
  const codes13 = ALL_VALID_CODES.filter(code => code.endsWith('13'));
  const collected13 = codes13.filter(code => collection[code]?.status === 'collected');
  progress['master_13'] = { current: collected13.length, target: codes13.length };

  // Special sections
  const fifaCat = CATEGORIES.find(c => c.id === 'fifa_world_cup');
  const fifaCollected = fifaCat.stickers.filter(code => collection[code]?.status === 'collected');
  progress['fifa_complete'] = { current: fifaCollected.length, target: fifaCat.stickers.length };

  const cocaCat = CATEGORIES.find(c => c.id === 'coca-cola');
  const cocaCollected = cocaCat.stickers.filter(code => collection[code]?.status === 'collected');
  progress['coca_complete'] = { current: cocaCollected.length, target: cocaCat.stickers.length };

  // Groups
  const groups = [...new Set(CATEGORIES.map(c => c.group).filter(g => g.startsWith('Grupo')))];
  let maxGroupProgress = 0;
  let completeGroups = 0;
  for (const group of groups) {
    const groupCats = CATEGORIES.filter(c => c.group === group);
    const totalInGroup = groupCats.reduce((acc, c) => acc + c.stickers.length, 0);
    const collectedInGroup = groupCats.reduce((acc, c) => 
      acc + c.stickers.filter(code => collection[code]?.status === 'collected').length, 0
    );
    if (collectedInGroup === totalInGroup) {
      maxGroupProgress = totalInGroup;
      completeGroups++;
    }
    else if (collectedInGroup > maxGroupProgress) maxGroupProgress = collectedInGroup;
  }
  progress['group_complete'] = { current: completeGroups >= 1 ? 1 : 0, target: 1 };
  progress['first_100_percent_group'] = { current: completeGroups, target: 3 };

  // Euro complete progress
  const euroCats = ['rep._tcheca', 'suíça', 'escócia', 'bósnia', 'alemanha', 'holanda', 'suécia', 'bélgica', 'espanha', 'frança', 'noruega', 'áustria', 'portugal', 'inglaterra', 'croácia'];
  let euroCollected = 0;
  let euroTotal = 0;
  euroCats.forEach(catId => {
    const cat = CATEGORIES.find(c => c.id === catId);
    if (cat) {
      euroTotal += cat.stickers.length;
      euroCollected += cat.stickers.filter(code => collection[code]?.status === 'collected').length;
    }
  });
  progress['euro_complete'] = { current: euroCollected, target: euroTotal };

  // World Tour progress
  const saCats = ['brasil', 'argentina', 'uruguai', 'paraguai', 'equador', 'colômbia'];
  const naCats = ['méxico', 'canadá', 'estados_unidos', 'haiti', 'curaçao', 'panamá'];
  const euCats = ['rep._tcheca', 'suíça', 'escócia', 'bósnia', 'alemanha', 'holanda', 'suécia', 'bélgica', 'espanha', 'frança', 'noruega', 'áustria', 'portugal', 'inglaterra', 'croácia'];
  const afCats = ['áfrica_do_sul', 'marrocos', 'costa_do_marfim', 'tunísia', 'egito', 'senegal', 'argélia', 'congo', 'cabo_verde', 'gana'];
  const asCats = ['coreia_do_sul', 'catar', 'austrália', 'japão', 'irã', 'nova_zelândia', 'arábia_saudita', 'iraque', 'jordânia', 'uzbequistão'];
  const hasSa = saCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  const hasNa = naCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  const hasEu = euCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  const hasAf = afCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  const hasAs = asCats.some(catId => CATEGORIES.find(c => c.id === catId)?.stickers.some(code => collection[code]?.status === 'collected'));
  let continents = 0;
  if (hasSa) continents++;
  if (hasNa) continents++;
  if (hasEu) continents++;
  if (hasAf) continents++;
  if (hasAs) continents++;
  progress['world_tour'] = { current: continents, target: 5 };

  // Repescagem complete progress
  const playoffCats = ['rep._tcheca', 'bósnia', 'turquia', 'suécia', 'iraque', 'congo'];
  let playoffCollected = 0;
  let playoffTotal = 0;
  playoffCats.forEach(catId => {
    const cat = CATEGORIES.find(c => c.id === catId);
    if (cat) {
      playoffTotal += cat.stickers.length;
      playoffCollected += cat.stickers.filter(code => collection[code]?.status === 'collected').length;
    }
  });
  progress['repescagem_complete'] = { current: playoffCollected, target: playoffTotal };

  // Hosts complete progress
  const mexStickers = CATEGORIES.find(c => c.id === 'méxico')?.stickers || [];
  const canStickers = CATEGORIES.find(c => c.id === 'canadá')?.stickers || [];
  const usaStickers = CATEGORIES.find(c => c.id === 'estados_unidos')?.stickers || [];
  const hostsTotalStickers = [...mexStickers, ...canStickers, ...usaStickers];
  const hostsCollected = hostsTotalStickers.filter(code => collection[code]?.status === 'collected').length;
  progress['hosts_complete'] = { current: hostsCollected, target: hostsTotalStickers.length };

  return progress;
};

export const calculateCompletionEstimate = (coladas, total, stickersPerPack = 7) => {
  const targetPercent = 0.60;
  const targetStickers = Math.floor(total * targetPercent);
  
  // 1. Total needed from 0 to 60%
  let harmonicSumTotal = 0;
  for (let i = (total - targetStickers) + 1; i <= total; i++) {
    harmonicSumTotal += 1 / i;
  }
  const totalPacks = Math.ceil((total * harmonicSumTotal) / stickersPerPack);

  // 2. Remaining needed from current to 60%
  if (coladas >= targetStickers) return { remaining: 0, total: totalPacks };
  
  let harmonicSumRemaining = 0;
  const start = total - targetStickers;
  const end = total - coladas;
  for (let i = start + 1; i <= end; i++) {
    harmonicSumRemaining += 1 / i;
  }
  
  const remainingPacks = Math.ceil((total * harmonicSumRemaining) / stickersPerPack);
  
  return { remaining: remainingPacks, total: totalPacks };
};
